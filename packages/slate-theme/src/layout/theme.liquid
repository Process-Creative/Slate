<!DOCTYPE HTML>
<html class="js-unavailable" lang="{{ shop.locale }}">
  <head>
    {%- render 'tool.head-tags' -%}
    {{ content_for_header }}
  </head>
  
  <body>
    {% section 'navigation-topbar' %}
    {% section 'navigation-header' %}

    <main role="main" class="c-main" id="MainContent">
      {{ content_for_layout }}
    </main>

    {%- render 'tool.body-end-tags' -%}
  </body>
</html>
